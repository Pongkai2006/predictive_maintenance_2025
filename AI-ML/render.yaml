services:
  - type: web
    name: predictive-maintenance-backend
    env: node
    region: singapore
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: node websocket_server.js
    envVars:
      - key: PORT
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
